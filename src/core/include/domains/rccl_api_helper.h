/**
 * @file rccl_api_helper.h
 * @brief Helper functions for managing RCCL API calls in the profiling framework.
 *
 * This file contains various utility functions used to handle RCCL API calls 
 * within the profiling system, including retrieving argument values and 
 * managing function identifiers.
 * 
 * @note THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY THE GILDA TOOL.
 * DO NOT MODIFY UNLESS YOU KNOW WHAT YOU ARE DOING.
 * ANY CHANGES MAY BE OVERWRITTEN BY SUBSEQUENT RUNS OF GILDA. 
 * 
 */

#ifndef RCCL_API_HELPER_H
#define RCCL_API_HELPER_H

#include <string.h>
#include <stdbool.h>
#include "domains/fun_proto/rccl_profiled_functions.h"
#include "domains/minimal_abi/hip_minimal_abi.h"
#include "domains/minimal_abi/rccl_minimal_abi.h" 

#define RCCL_STRING_SIZE_MAX 128

#ifdef ADD_API_PREFIX
#undef ADD_API_PREFIX
#endif
#define ADD_API_PREFIX(str) RCCL_API_##str


#define FOR_EACH_RCCL_FUNC(macro) \
macro(ncclGetLastError)                            \
macro(ncclGetErrorString)                          \
macro(ncclGetVersion)                              \
macro(ncclCommFinalize)                            \
macro(ncclBcast)                                   \
macro(ncclGroupStart)                              \
macro(ncclAllToAll)                                \
macro(ncclAllGather)                               \
macro(ncclGroupSimulateEnd)                        \
macro(ncclCommWindowRegister)                      \
macro(ncclCommDeregister)                          \
macro(mscclUnloadAlgo)                             \
macro(ncclRecv)                                    \
macro(ncclGroupEnd)                                \
macro(ncclRedOpDestroy)                            \
macro(ncclCommCount)                               \
macro(ncclScatter)                                 \
macro(ncclCommShrink)                              \
macro(ncclCommInitRankScalable)                    \
macro(ncclCommRegister)                            \
macro(ncclCommGetAsyncError)                       \
macro(ncclMemFree)                                 \
macro(ncclCommInitAll)                             \
macro(ncclCommCuDevice)                            \
macro(ncclCommSplit)                               \
macro(mscclRunAlgo)                                \
macro(ncclCommUserRank)                            \
macro(ncclRedOpCreatePreMulSum)                    \
macro(ncclAllReduce)                               \
macro(ncclCommInitRank)                            \
macro(ncclBroadcast)                               \
macro(ncclAllToAllv)                               \
macro(mscclLoadAlgo)                               \
macro(ncclMemAlloc)                                \
macro(ncclCommAbort)                               \
macro(ncclGetUniqueId)                             \
macro(ncclReduceScatter)                           \
macro(ncclGather)                                  \
macro(ncclCommWindowDeregister)                    \
macro(ncclSend)                                    \
macro(ncclCommInitRankConfig)                      \
macro(ncclCommDestroy)                             \
macro(ncclAllReduceWithBias)                       \
macro(ncclReduce)                                  \
macro(ncclResetDebugInit)                          \


/**
 * @enum rccl_api_id_t 
 * @brief Enumeration of RCCL API function identifiers.
 *
 * This enumeration defines unique identifiers for various RCCL API functions. 
 * These identifiers are used for profiling, tracking, and identifying specific RCCL function calls.
 */
typedef enum rccl_api_id_e {
    FOR_EACH_RCCL_FUNC(GET_FUNC_API_ID)
    RCCL_API_ID_NB_FUNCTION,
    RCCL_API_ID_UNKNOWN,
} rccl_api_id_t;


/**
 * @brief Retrieves the function name corresponding to a given RCCL API function ID.
 *
 * This function maps a RCCL API function identifier (`rccl_api_id_t`) to its corresponding function name
 * as a string. If the provided function ID does not match any known functions, the function returns `NULL`.
 *
 * @param id The RCCL API function identifier of type `rccl_api_id_t`.
 * @return A constant string representing the function name, or `NULL` if the ID is unknown.
 */
static inline const char* get_rccl_funame_by_id(rccl_api_id_t id) 
{
    switch(id) {
        FOR_EACH_RCCL_FUNC(GET_FUNAME_BY_ID_OF)
        default : return NULL;
    }
    return NULL;
}


/**
 * @brief Retrieves the function pointer corresponding to a given RCCL API function ID.
 *
 * This function maps a RCCL API function ID (`rccl_api_id_t`) to its corresponding function address.
 * If the provided function ID does not match any known functions, the function returns `NULL`.
 *
 * @param id The function ID of type `rccl_api_id_t`.
 * @return A pointer to the corresponding RCCL API function, or `NULL` if not found.
 */
static inline void* get_rccl_funaddr_by_id(rccl_api_id_t id) 
{
    switch(id) {
        FOR_EACH_RCCL_FUNC(GET_FUNADDR_BY_ID_OF)
        default : return NULL;
    }
    return NULL;
}


/**
 * @brief Retrieves the RCCL API function ID corresponding to a given function name.
 *
 * This function maps a RCCL API function name (string) to its corresponding function ID (`rccl_api_id_t`).
 * If the provided function name does not match any known functions, the function returns `RCCL_API_ID_UNKNOWN`.
 *
 * @param name The function name as a null-terminated string.
 * @return The corresponding RCCL API function ID of type `rccl_api_id_t`, or `RCCL_API_ID_UNKNOWN` if not found.
 */
static inline rccl_api_id_t get_rccl_funid_by_name(const char* name) 
{
    if (name == NULL) return RCCL_API_ID_UNKNOWN;
    FOR_EACH_RCCL_FUNC(GET_FUNID_BY_NAME_OF)
    return RCCL_API_ID_UNKNOWN;
}


// RCCL API Args Data
/**
 * @brief Structure to hold the arguments for the `ncclGetLastError` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGetLastError` function call.
 *
 * @struct args_ncclGetLastError_t
 *
 * @note 
 *	const char *
 *	ncclGetLastError (
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	char * retval;
	struct {
		char  val;
	} retval__ref;
} args_ncclGetLastError_t;

#define GET_ARGS_VALUE_ncclGetLastError(activity) { \
	args_ncclGetLastError_t* args = (args_ncclGetLastError_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
};

#define GET_PTRS_RET_VALUE_ncclGetLastError(args) { \
	args_ncclGetLastError_t* pargs = (args_ncclGetLastError_t*) args; \
	if (pargs->retval != NULL) { \
		pargs->retval__ref.val = *pargs->retval; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclGetErrorString` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGetErrorString` function call.
 *
 * @struct args_ncclGetErrorString_t
 *
 * @note 
 *	const char *
 *	ncclGetErrorString (
 *			ncclResult_t result (enum ncclResult_t)
 *	)
 */
typedef struct {
	ncclResult_t result;
	char * retval;
	struct {
		char  val;
	} retval__ref;
} args_ncclGetErrorString_t;

#define GET_ARGS_VALUE_ncclGetErrorString(activity) { \
	args_ncclGetErrorString_t* args = (args_ncclGetErrorString_t*) activity->args; \
	args->result = (ncclResult_t) result; \
};

#define GET_PTRS_RET_VALUE_ncclGetErrorString(args) { \
	args_ncclGetErrorString_t* pargs = (args_ncclGetErrorString_t*) args; \
	if (pargs->retval != NULL) { \
		pargs->retval__ref.val = *pargs->retval; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclGetVersion` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGetVersion` function call.
 *
 * @struct args_ncclGetVersion_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGetVersion (
 *			int * version (int *)
 *	)
 */
typedef struct {
	int * version;
	struct {
		int  val;
	} version__ref;
	ncclResult_t retval;
} args_ncclGetVersion_t;

#define GET_ARGS_VALUE_ncclGetVersion(activity) { \
	args_ncclGetVersion_t* args = (args_ncclGetVersion_t*) activity->args; \
	args->version = (int *) version; \
};

#define GET_PTRS_VALUE_ncclGetVersion(args) { \
	args_ncclGetVersion_t* pargs = (args_ncclGetVersion_t*) args; \
	if (pargs->version != NULL) { \
		pargs->version__ref.val = *pargs->version; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommFinalize` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommFinalize` function call.
 *
 * @struct args_ncclCommFinalize_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommFinalize (
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	ncclResult_t retval;
} args_ncclCommFinalize_t;

#define GET_ARGS_VALUE_ncclCommFinalize(activity) { \
	args_ncclCommFinalize_t* args = (args_ncclCommFinalize_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
};

/**
 * @brief Structure to hold the arguments for the `ncclBcast` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclBcast` function call.
 *
 * @struct args_ncclBcast_t
 *
 * @note 
 *	ncclResult_t
 *	ncclBcast (
 *			void * buff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int root (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * buff;
	size_t count;
	ncclDataType_t datatype;
	int root;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclBcast_t;

#define GET_ARGS_VALUE_ncclBcast(activity) { \
	args_ncclBcast_t* args = (args_ncclBcast_t*) activity->args; \
	args->buff = (void *) buff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->root = (int) root; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclGroupStart` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGroupStart` function call.
 *
 * @struct args_ncclGroupStart_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGroupStart (
 *	)
 */
typedef struct {
	ncclResult_t retval;
} args_ncclGroupStart_t;

/**
 * @brief Structure to hold the arguments for the `ncclAllToAll` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclAllToAll` function call.
 *
 * @struct args_ncclAllToAll_t
 *
 * @note 
 *	ncclResult_t
 *	ncclAllToAll (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclAllToAll_t;

#define GET_ARGS_VALUE_ncclAllToAll(activity) { \
	args_ncclAllToAll_t* args = (args_ncclAllToAll_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclAllGather` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclAllGather` function call.
 *
 * @struct args_ncclAllGather_t
 *
 * @note 
 *	ncclResult_t
 *	ncclAllGather (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t sendcount (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t sendcount;
	ncclDataType_t datatype;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclAllGather_t;

#define GET_ARGS_VALUE_ncclAllGather(activity) { \
	args_ncclAllGather_t* args = (args_ncclAllGather_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->sendcount = (size_t) sendcount; \
	args->datatype = (ncclDataType_t) datatype; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclGroupSimulateEnd` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGroupSimulateEnd` function call.
 *
 * @struct args_ncclGroupSimulateEnd_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGroupSimulateEnd (
 *			ncclSimInfo_t * simInfo (struct ncclSimInfo_v22200*)
 *	)
 */
typedef struct {
	ncclSimInfo_t * simInfo;
	struct {
		ncclSimInfo_t  val;
	} simInfo__ref;
	ncclResult_t retval;
} args_ncclGroupSimulateEnd_t;

#define GET_ARGS_VALUE_ncclGroupSimulateEnd(activity) { \
	args_ncclGroupSimulateEnd_t* args = (args_ncclGroupSimulateEnd_t*) activity->args; \
	args->simInfo = (ncclSimInfo_t *) simInfo; \
};

#define GET_PTRS_VALUE_ncclGroupSimulateEnd(args) { \
	args_ncclGroupSimulateEnd_t* pargs = (args_ncclGroupSimulateEnd_t*) args; \
	if (pargs->simInfo != NULL) { \
		pargs->simInfo__ref.val = *pargs->simInfo; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommWindowRegister` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommWindowRegister` function call.
 *
 * @struct args_ncclCommWindowRegister_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommWindowRegister (
 *			ncclComm_t comm (struct ncclComm *)
 *			void * buff (void *)
 *			size_t size (unsigned long)
 *			ncclWindow_t * win (struct ncclWindow **)
 *			int winFlags (int)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	void * buff;
	size_t size;
	ncclWindow_t * win;
	struct {
		ncclWindow_t  val;
	} win__ref;
	int winFlags;
	ncclResult_t retval;
} args_ncclCommWindowRegister_t;

#define GET_ARGS_VALUE_ncclCommWindowRegister(activity) { \
	args_ncclCommWindowRegister_t* args = (args_ncclCommWindowRegister_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->buff = (void *) buff; \
	args->size = (size_t) size; \
	args->win = (ncclWindow_t *) win; \
	args->winFlags = (int) winFlags; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommDeregister` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommDeregister` function call.
 *
 * @struct args_ncclCommDeregister_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommDeregister (
 *			const ncclComm_t comm (const struct ncclComm *)
 *			void * handle (void *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	void * handle;
	ncclResult_t retval;
} args_ncclCommDeregister_t;

#define GET_ARGS_VALUE_ncclCommDeregister(activity) { \
	args_ncclCommDeregister_t* args = (args_ncclCommDeregister_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->handle = (void *) handle; \
};

/**
 * @brief Structure to hold the arguments for the `mscclUnloadAlgo` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `mscclUnloadAlgo` function call.
 *
 * @struct args_mscclUnloadAlgo_t
 *
 * @note 
 *	ncclResult_t
 *	mscclUnloadAlgo (
 *			mscclAlgoHandle_t mscclAlgoHandle (int)
 *	)
 */
typedef struct {
	mscclAlgoHandle_t mscclAlgoHandle;
	ncclResult_t retval;
} args_mscclUnloadAlgo_t;

#define GET_ARGS_VALUE_mscclUnloadAlgo(activity) { \
	args_mscclUnloadAlgo_t* args = (args_mscclUnloadAlgo_t*) activity->args; \
	args->mscclAlgoHandle = (mscclAlgoHandle_t) mscclAlgoHandle; \
};

/**
 * @brief Structure to hold the arguments for the `ncclRecv` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclRecv` function call.
 *
 * @struct args_ncclRecv_t
 *
 * @note 
 *	ncclResult_t
 *	ncclRecv (
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int peer (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	int peer;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclRecv_t;

#define GET_ARGS_VALUE_ncclRecv(activity) { \
	args_ncclRecv_t* args = (args_ncclRecv_t*) activity->args; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->peer = (int) peer; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclGroupEnd` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGroupEnd` function call.
 *
 * @struct args_ncclGroupEnd_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGroupEnd (
 *	)
 */
typedef struct {
	ncclResult_t retval;
} args_ncclGroupEnd_t;

/**
 * @brief Structure to hold the arguments for the `ncclRedOpDestroy` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclRedOpDestroy` function call.
 *
 * @struct args_ncclRedOpDestroy_t
 *
 * @note 
 *	ncclResult_t
 *	ncclRedOpDestroy (
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclRedOp_t op;
	ncclComm_t comm;
	ncclResult_t retval;
} args_ncclRedOpDestroy_t;

#define GET_ARGS_VALUE_ncclRedOpDestroy(activity) { \
	args_ncclRedOpDestroy_t* args = (args_ncclRedOpDestroy_t*) activity->args; \
	args->op = (ncclRedOp_t) op; \
	args->comm = (ncclComm_t) comm; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommCount` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommCount` function call.
 *
 * @struct args_ncclCommCount_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommCount (
 *			const ncclComm_t comm (const struct ncclComm *)
 *			int * count (int *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	int * count;
	struct {
		int  val;
	} count__ref;
	ncclResult_t retval;
} args_ncclCommCount_t;

#define GET_ARGS_VALUE_ncclCommCount(activity) { \
	args_ncclCommCount_t* args = (args_ncclCommCount_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->count = (int *) count; \
};

#define GET_PTRS_VALUE_ncclCommCount(args) { \
	args_ncclCommCount_t* pargs = (args_ncclCommCount_t*) args; \
	if (pargs->count != NULL) { \
		pargs->count__ref.val = *pargs->count; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclScatter` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclScatter` function call.
 *
 * @struct args_ncclScatter_t
 *
 * @note 
 *	ncclResult_t
 *	ncclScatter (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t recvcount (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int root (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t recvcount;
	ncclDataType_t datatype;
	int root;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclScatter_t;

#define GET_ARGS_VALUE_ncclScatter(activity) { \
	args_ncclScatter_t* args = (args_ncclScatter_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->recvcount = (size_t) recvcount; \
	args->datatype = (ncclDataType_t) datatype; \
	args->root = (int) root; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommShrink` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommShrink` function call.
 *
 * @struct args_ncclCommShrink_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommShrink (
 *			ncclComm_t comm (struct ncclComm *)
 *			int * excludeRanksList (int *)
 *			int excludeRanksCount (int)
 *			ncclComm_t * newcomm (struct ncclComm **)
 *			ncclConfig_t * config (struct ncclConfig_v22700*)
 *			int shrinkFlags (int)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	int * excludeRanksList;
	struct {
		int  val;
	} excludeRanksList__ref;
	int excludeRanksCount;
	ncclComm_t * newcomm;
	struct {
		ncclComm_t  val;
	} newcomm__ref;
	ncclConfig_t * config;
	struct {
		ncclConfig_t  val;
	} config__ref;
	int shrinkFlags;
	ncclResult_t retval;
} args_ncclCommShrink_t;

#define GET_ARGS_VALUE_ncclCommShrink(activity) { \
	args_ncclCommShrink_t* args = (args_ncclCommShrink_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->excludeRanksList = (int *) excludeRanksList; \
	args->excludeRanksCount = (int) excludeRanksCount; \
	args->newcomm = (ncclComm_t *) newcomm; \
	args->config = (ncclConfig_t *) config; \
	args->shrinkFlags = (int) shrinkFlags; \
};

#define GET_PTRS_VALUE_ncclCommShrink(args) { \
	args_ncclCommShrink_t* pargs = (args_ncclCommShrink_t*) args; \
	if (pargs->excludeRanksList != NULL) { \
		pargs->excludeRanksList__ref.val = *pargs->excludeRanksList; \
	} \
	if (pargs->newcomm != NULL) { \
		pargs->newcomm__ref.val = *pargs->newcomm; \
	} \
	if (pargs->config != NULL) { \
		pargs->config__ref.val = *pargs->config; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommInitRankScalable` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommInitRankScalable` function call.
 *
 * @struct args_ncclCommInitRankScalable_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommInitRankScalable (
 *			ncclComm_t * newcomm (struct ncclComm **)
 *			int nranks (int)
 *			int myrank (int)
 *			int nId (int)
 *			ncclUniqueId * commIds (struct ncclUniqueId*)
 *			ncclConfig_t * config (struct ncclConfig_v22700*)
 *	)
 */
typedef struct {
	ncclComm_t * newcomm;
	struct {
		ncclComm_t  val;
	} newcomm__ref;
	int nranks;
	int myrank;
	int nId;
	ncclUniqueId * commIds;
	struct {
		ncclUniqueId  val;
	} commIds__ref;
	ncclConfig_t * config;
	struct {
		ncclConfig_t  val;
	} config__ref;
	ncclResult_t retval;
} args_ncclCommInitRankScalable_t;

#define GET_ARGS_VALUE_ncclCommInitRankScalable(activity) { \
	args_ncclCommInitRankScalable_t* args = (args_ncclCommInitRankScalable_t*) activity->args; \
	args->newcomm = (ncclComm_t *) newcomm; \
	args->nranks = (int) nranks; \
	args->myrank = (int) myrank; \
	args->nId = (int) nId; \
	args->commIds = (ncclUniqueId *) commIds; \
	args->config = (ncclConfig_t *) config; \
};

#define GET_PTRS_VALUE_ncclCommInitRankScalable(args) { \
	args_ncclCommInitRankScalable_t* pargs = (args_ncclCommInitRankScalable_t*) args; \
	if (pargs->newcomm != NULL) { \
		pargs->newcomm__ref.val = *pargs->newcomm; \
	} \
	if (pargs->commIds != NULL) { \
		pargs->commIds__ref.val = *pargs->commIds; \
	} \
	if (pargs->config != NULL) { \
		pargs->config__ref.val = *pargs->config; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommRegister` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommRegister` function call.
 *
 * @struct args_ncclCommRegister_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommRegister (
 *			const ncclComm_t comm (const struct ncclComm *)
 *			void * buff (void *)
 *			size_t size (unsigned long)
 *			void ** handle (void **)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	void * buff;
	size_t size;
	void ** handle;
	struct {
		void* ptr1;
	} handle__ref;
	ncclResult_t retval;
} args_ncclCommRegister_t;

#define GET_ARGS_VALUE_ncclCommRegister(activity) { \
	args_ncclCommRegister_t* args = (args_ncclCommRegister_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->buff = (void *) buff; \
	args->size = (size_t) size; \
	args->handle = (void **) handle; \
};

#define GET_PTRS_VALUE_ncclCommRegister(args) { \
	args_ncclCommRegister_t* pargs = (args_ncclCommRegister_t*) args; \
	if (pargs->handle != NULL) { \
		pargs->handle__ref.ptr1 = *pargs->handle; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommGetAsyncError` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommGetAsyncError` function call.
 *
 * @struct args_ncclCommGetAsyncError_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommGetAsyncError (
 *			ncclComm_t comm (struct ncclComm *)
 *			ncclResult_t * asyncError (enum ncclResult_t*)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	ncclResult_t * asyncError;
	struct {
		ncclResult_t  val;
	} asyncError__ref;
	ncclResult_t retval;
} args_ncclCommGetAsyncError_t;

#define GET_ARGS_VALUE_ncclCommGetAsyncError(activity) { \
	args_ncclCommGetAsyncError_t* args = (args_ncclCommGetAsyncError_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->asyncError = (ncclResult_t *) asyncError; \
};

#define GET_PTRS_VALUE_ncclCommGetAsyncError(args) { \
	args_ncclCommGetAsyncError_t* pargs = (args_ncclCommGetAsyncError_t*) args; \
	if (pargs->asyncError != NULL) { \
		pargs->asyncError__ref.val = *pargs->asyncError; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclMemFree` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclMemFree` function call.
 *
 * @struct args_ncclMemFree_t
 *
 * @note 
 *	ncclResult_t
 *	ncclMemFree (
 *			void * ptr (void *)
 *	)
 */
typedef struct {
	void * ptr;
	ncclResult_t retval;
} args_ncclMemFree_t;

#define GET_ARGS_VALUE_ncclMemFree(activity) { \
	args_ncclMemFree_t* args = (args_ncclMemFree_t*) activity->args; \
	args->ptr = (void *) ptr; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommInitAll` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommInitAll` function call.
 *
 * @struct args_ncclCommInitAll_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommInitAll (
 *			ncclComm_t * comm (struct ncclComm **)
 *			int ndev (int)
 *			const int * devlist (const int *)
 *	)
 */
typedef struct {
	ncclComm_t * comm;
	struct {
		ncclComm_t  val;
	} comm__ref;
	int ndev;
	int * devlist;
	struct {
		int  val;
	} devlist__ref;
	ncclResult_t retval;
} args_ncclCommInitAll_t;

#define GET_ARGS_VALUE_ncclCommInitAll(activity) { \
	args_ncclCommInitAll_t* args = (args_ncclCommInitAll_t*) activity->args; \
	args->comm = (ncclComm_t *) comm; \
	args->ndev = (int) ndev; \
	args->devlist = (int *) devlist; \
};

#define GET_PTRS_VALUE_ncclCommInitAll(args) { \
	args_ncclCommInitAll_t* pargs = (args_ncclCommInitAll_t*) args; \
	if (pargs->comm != NULL) { \
		pargs->comm__ref.val = *pargs->comm; \
	} \
	if (pargs->devlist != NULL) { \
		pargs->devlist__ref.val = *pargs->devlist; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommCuDevice` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommCuDevice` function call.
 *
 * @struct args_ncclCommCuDevice_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommCuDevice (
 *			const ncclComm_t comm (const struct ncclComm *)
 *			int * device (int *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	int * device;
	struct {
		int  val;
	} device__ref;
	ncclResult_t retval;
} args_ncclCommCuDevice_t;

#define GET_ARGS_VALUE_ncclCommCuDevice(activity) { \
	args_ncclCommCuDevice_t* args = (args_ncclCommCuDevice_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->device = (int *) device; \
};

#define GET_PTRS_VALUE_ncclCommCuDevice(args) { \
	args_ncclCommCuDevice_t* pargs = (args_ncclCommCuDevice_t*) args; \
	if (pargs->device != NULL) { \
		pargs->device__ref.val = *pargs->device; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommSplit` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommSplit` function call.
 *
 * @struct args_ncclCommSplit_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommSplit (
 *			ncclComm_t comm (struct ncclComm *)
 *			int color (int)
 *			int key (int)
 *			ncclComm_t * newcomm (struct ncclComm **)
 *			ncclConfig_t * config (struct ncclConfig_v22700*)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	int color;
	int key;
	ncclComm_t * newcomm;
	struct {
		ncclComm_t  val;
	} newcomm__ref;
	ncclConfig_t * config;
	struct {
		ncclConfig_t  val;
	} config__ref;
	ncclResult_t retval;
} args_ncclCommSplit_t;

#define GET_ARGS_VALUE_ncclCommSplit(activity) { \
	args_ncclCommSplit_t* args = (args_ncclCommSplit_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->color = (int) color; \
	args->key = (int) key; \
	args->newcomm = (ncclComm_t *) newcomm; \
	args->config = (ncclConfig_t *) config; \
};

#define GET_PTRS_VALUE_ncclCommSplit(args) { \
	args_ncclCommSplit_t* pargs = (args_ncclCommSplit_t*) args; \
	if (pargs->newcomm != NULL) { \
		pargs->newcomm__ref.val = *pargs->newcomm; \
	} \
	if (pargs->config != NULL) { \
		pargs->config__ref.val = *pargs->config; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `mscclRunAlgo` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `mscclRunAlgo` function call.
 *
 * @struct args_mscclRunAlgo_t
 *
 * @note 
 *	ncclResult_t
 *	mscclRunAlgo (
 *			const void * sendBuff (const void *)
 *			const size_t[] sendCounts (const unsigned long[])
 *			const size_t[] sDisPls (const unsigned long[])
 *			void * recvBuff (void *)
 *			const size_t[] recvCounts (const unsigned long[])
 *			const size_t[] rDisPls (const unsigned long[])
 *			size_t count (unsigned long)
 *			ncclDataType_t dataType (enum ncclDataType_t)
 *			int root (int)
 *			int peer (int)
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			mscclAlgoHandle_t mscclAlgoHandle (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendBuff;
	size_t(* sendCounts);
	struct {
		size_t val;
	} sendCounts__ref;
	size_t(* sDisPls);
	struct {
		size_t val;
	} sDisPls__ref;
	void * recvBuff;
	size_t(* recvCounts);
	struct {
		size_t val;
	} recvCounts__ref;
	size_t(* rDisPls);
	struct {
		size_t val;
	} rDisPls__ref;
	size_t count;
	ncclDataType_t dataType;
	int root;
	int peer;
	ncclRedOp_t op;
	mscclAlgoHandle_t mscclAlgoHandle;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_mscclRunAlgo_t;

#define GET_ARGS_VALUE_mscclRunAlgo(activity) { \
	args_mscclRunAlgo_t* args = (args_mscclRunAlgo_t*) activity->args; \
	args->sendBuff = (void *) sendBuff; \
	args->sendCounts = (size_t(*)) sendCounts; \
	args->sDisPls = (size_t(*)) sDisPls; \
	args->recvBuff = (void *) recvBuff; \
	args->recvCounts = (size_t(*)) recvCounts; \
	args->rDisPls = (size_t(*)) rDisPls; \
	args->count = (size_t) count; \
	args->dataType = (ncclDataType_t) dataType; \
	args->root = (int) root; \
	args->peer = (int) peer; \
	args->op = (ncclRedOp_t) op; \
	args->mscclAlgoHandle = (mscclAlgoHandle_t) mscclAlgoHandle; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

#define GET_PTRS_VALUE_mscclRunAlgo(args) { \
	args_mscclRunAlgo_t* pargs = (args_mscclRunAlgo_t*) args; \
	if (pargs->sendCounts != NULL) { \
		pargs->sendCounts__ref.val = *pargs->sendCounts; \
	} \
	if (pargs->sDisPls != NULL) { \
		pargs->sDisPls__ref.val = *pargs->sDisPls; \
	} \
	if (pargs->recvCounts != NULL) { \
		pargs->recvCounts__ref.val = *pargs->recvCounts; \
	} \
	if (pargs->rDisPls != NULL) { \
		pargs->rDisPls__ref.val = *pargs->rDisPls; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommUserRank` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommUserRank` function call.
 *
 * @struct args_ncclCommUserRank_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommUserRank (
 *			const ncclComm_t comm (const struct ncclComm *)
 *			int * rank (int *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	int * rank;
	struct {
		int  val;
	} rank__ref;
	ncclResult_t retval;
} args_ncclCommUserRank_t;

#define GET_ARGS_VALUE_ncclCommUserRank(activity) { \
	args_ncclCommUserRank_t* args = (args_ncclCommUserRank_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->rank = (int *) rank; \
};

#define GET_PTRS_VALUE_ncclCommUserRank(args) { \
	args_ncclCommUserRank_t* pargs = (args_ncclCommUserRank_t*) args; \
	if (pargs->rank != NULL) { \
		pargs->rank__ref.val = *pargs->rank; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclRedOpCreatePreMulSum` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclRedOpCreatePreMulSum` function call.
 *
 * @struct args_ncclRedOpCreatePreMulSum_t
 *
 * @note 
 *	ncclResult_t
 *	ncclRedOpCreatePreMulSum (
 *			ncclRedOp_t * op (enum ncclRedOp_t*)
 *			void * scalar (void *)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclScalarResidence_t residence (enum ncclScalarResidence_t)
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclRedOp_t * op;
	struct {
		ncclRedOp_t  val;
	} op__ref;
	void * scalar;
	ncclDataType_t datatype;
	ncclScalarResidence_t residence;
	ncclComm_t comm;
	ncclResult_t retval;
} args_ncclRedOpCreatePreMulSum_t;

#define GET_ARGS_VALUE_ncclRedOpCreatePreMulSum(activity) { \
	args_ncclRedOpCreatePreMulSum_t* args = (args_ncclRedOpCreatePreMulSum_t*) activity->args; \
	args->op = (ncclRedOp_t *) op; \
	args->scalar = (void *) scalar; \
	args->datatype = (ncclDataType_t) datatype; \
	args->residence = (ncclScalarResidence_t) residence; \
	args->comm = (ncclComm_t) comm; \
};

#define GET_PTRS_VALUE_ncclRedOpCreatePreMulSum(args) { \
	args_ncclRedOpCreatePreMulSum_t* pargs = (args_ncclRedOpCreatePreMulSum_t*) args; \
	if (pargs->op != NULL) { \
		pargs->op__ref.val = *pargs->op; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclAllReduce` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclAllReduce` function call.
 *
 * @struct args_ncclAllReduce_t
 *
 * @note 
 *	ncclResult_t
 *	ncclAllReduce (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	ncclRedOp_t op;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclAllReduce_t;

#define GET_ARGS_VALUE_ncclAllReduce(activity) { \
	args_ncclAllReduce_t* args = (args_ncclAllReduce_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->op = (ncclRedOp_t) op; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommInitRank` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommInitRank` function call.
 *
 * @struct args_ncclCommInitRank_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommInitRank (
 *			ncclComm_t * comm (struct ncclComm **)
 *			int nranks (int)
 *			ncclUniqueId commId (struct ncclUniqueId)
 *			int rank (int)
 *	)
 */
typedef struct {
	ncclComm_t * comm;
	struct {
		ncclComm_t  val;
	} comm__ref;
	int nranks;
	ncclUniqueId commId;
	int rank;
	ncclResult_t retval;
} args_ncclCommInitRank_t;

#define GET_ARGS_VALUE_ncclCommInitRank(activity) { \
	args_ncclCommInitRank_t* args = (args_ncclCommInitRank_t*) activity->args; \
	args->comm = (ncclComm_t *) comm; \
	args->nranks = (int) nranks; \
	args->commId = (ncclUniqueId) commId; \
	args->rank = (int) rank; \
};

/**
 * @brief Structure to hold the arguments for the `ncclBroadcast` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclBroadcast` function call.
 *
 * @struct args_ncclBroadcast_t
 *
 * @note 
 *	ncclResult_t
 *	ncclBroadcast (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int root (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	int root;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclBroadcast_t;

#define GET_ARGS_VALUE_ncclBroadcast(activity) { \
	args_ncclBroadcast_t* args = (args_ncclBroadcast_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->root = (int) root; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclAllToAllv` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclAllToAllv` function call.
 *
 * @struct args_ncclAllToAllv_t
 *
 * @note 
 *	ncclResult_t
 *	ncclAllToAllv (
 *			const void * sendbuff (const void *)
 *			const size_t[] sendcounts (const unsigned long[])
 *			const size_t[] sdispls (const unsigned long[])
 *			void * recvbuff (void *)
 *			const size_t[] recvcounts (const unsigned long[])
 *			const size_t[] rdispls (const unsigned long[])
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	size_t(* sendcounts);
	struct {
		size_t val;
	} sendcounts__ref;
	size_t(* sdispls);
	struct {
		size_t val;
	} sdispls__ref;
	void * recvbuff;
	size_t(* recvcounts);
	struct {
		size_t val;
	} recvcounts__ref;
	size_t(* rdispls);
	struct {
		size_t val;
	} rdispls__ref;
	ncclDataType_t datatype;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclAllToAllv_t;

#define GET_ARGS_VALUE_ncclAllToAllv(activity) { \
	args_ncclAllToAllv_t* args = (args_ncclAllToAllv_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->sendcounts = (size_t(*)) sendcounts; \
	args->sdispls = (size_t(*)) sdispls; \
	args->recvbuff = (void *) recvbuff; \
	args->recvcounts = (size_t(*)) recvcounts; \
	args->rdispls = (size_t(*)) rdispls; \
	args->datatype = (ncclDataType_t) datatype; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

#define GET_PTRS_VALUE_ncclAllToAllv(args) { \
	args_ncclAllToAllv_t* pargs = (args_ncclAllToAllv_t*) args; \
	if (pargs->sendcounts != NULL) { \
		pargs->sendcounts__ref.val = *pargs->sendcounts; \
	} \
	if (pargs->sdispls != NULL) { \
		pargs->sdispls__ref.val = *pargs->sdispls; \
	} \
	if (pargs->recvcounts != NULL) { \
		pargs->recvcounts__ref.val = *pargs->recvcounts; \
	} \
	if (pargs->rdispls != NULL) { \
		pargs->rdispls__ref.val = *pargs->rdispls; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `mscclLoadAlgo` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `mscclLoadAlgo` function call.
 *
 * @struct args_mscclLoadAlgo_t
 *
 * @note 
 *	ncclResult_t
 *	mscclLoadAlgo (
 *			const char * mscclAlgoFilePath (const char *)
 *			mscclAlgoHandle_t * mscclAlgoHandle (int*)
 *			int rank (int)
 *	)
 */
typedef struct {
	char * mscclAlgoFilePath;
	struct {
		char  val;
	} mscclAlgoFilePath__ref;
	mscclAlgoHandle_t * mscclAlgoHandle;
	struct {
		mscclAlgoHandle_t  val;
	} mscclAlgoHandle__ref;
	int rank;
	ncclResult_t retval;
} args_mscclLoadAlgo_t;

#define GET_ARGS_VALUE_mscclLoadAlgo(activity) { \
	args_mscclLoadAlgo_t* args = (args_mscclLoadAlgo_t*) activity->args; \
	args->mscclAlgoFilePath = (char *) mscclAlgoFilePath; \
	args->mscclAlgoHandle = (mscclAlgoHandle_t *) mscclAlgoHandle; \
	args->rank = (int) rank; \
};

#define GET_PTRS_VALUE_mscclLoadAlgo(args) { \
	args_mscclLoadAlgo_t* pargs = (args_mscclLoadAlgo_t*) args; \
	if (pargs->mscclAlgoFilePath != NULL) { \
		pargs->mscclAlgoFilePath__ref.val = *pargs->mscclAlgoFilePath; \
	} \
	if (pargs->mscclAlgoHandle != NULL) { \
		pargs->mscclAlgoHandle__ref.val = *pargs->mscclAlgoHandle; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclMemAlloc` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclMemAlloc` function call.
 *
 * @struct args_ncclMemAlloc_t
 *
 * @note 
 *	ncclResult_t
 *	ncclMemAlloc (
 *			void ** ptr (void **)
 *			size_t size (unsigned long)
 *	)
 */
typedef struct {
	void ** ptr;
	struct {
		void* ptr1;
	} ptr__ref;
	size_t size;
	ncclResult_t retval;
} args_ncclMemAlloc_t;

#define GET_ARGS_VALUE_ncclMemAlloc(activity) { \
	args_ncclMemAlloc_t* args = (args_ncclMemAlloc_t*) activity->args; \
	args->ptr = (void **) ptr; \
	args->size = (size_t) size; \
};

#define GET_PTRS_VALUE_ncclMemAlloc(args) { \
	args_ncclMemAlloc_t* pargs = (args_ncclMemAlloc_t*) args; \
	if (pargs->ptr != NULL) { \
		pargs->ptr__ref.ptr1 = *pargs->ptr; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommAbort` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommAbort` function call.
 *
 * @struct args_ncclCommAbort_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommAbort (
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	ncclResult_t retval;
} args_ncclCommAbort_t;

#define GET_ARGS_VALUE_ncclCommAbort(activity) { \
	args_ncclCommAbort_t* args = (args_ncclCommAbort_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
};

/**
 * @brief Structure to hold the arguments for the `ncclGetUniqueId` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGetUniqueId` function call.
 *
 * @struct args_ncclGetUniqueId_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGetUniqueId (
 *			ncclUniqueId * uniqueId (struct ncclUniqueId*)
 *	)
 */
typedef struct {
	ncclUniqueId * uniqueId;
	struct {
		ncclUniqueId  val;
	} uniqueId__ref;
	ncclResult_t retval;
} args_ncclGetUniqueId_t;

#define GET_ARGS_VALUE_ncclGetUniqueId(activity) { \
	args_ncclGetUniqueId_t* args = (args_ncclGetUniqueId_t*) activity->args; \
	args->uniqueId = (ncclUniqueId *) uniqueId; \
};

#define GET_PTRS_VALUE_ncclGetUniqueId(args) { \
	args_ncclGetUniqueId_t* pargs = (args_ncclGetUniqueId_t*) args; \
	if (pargs->uniqueId != NULL) { \
		pargs->uniqueId__ref.val = *pargs->uniqueId; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclReduceScatter` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclReduceScatter` function call.
 *
 * @struct args_ncclReduceScatter_t
 *
 * @note 
 *	ncclResult_t
 *	ncclReduceScatter (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t recvcount (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t recvcount;
	ncclDataType_t datatype;
	ncclRedOp_t op;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclReduceScatter_t;

#define GET_ARGS_VALUE_ncclReduceScatter(activity) { \
	args_ncclReduceScatter_t* args = (args_ncclReduceScatter_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->recvcount = (size_t) recvcount; \
	args->datatype = (ncclDataType_t) datatype; \
	args->op = (ncclRedOp_t) op; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclGather` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclGather` function call.
 *
 * @struct args_ncclGather_t
 *
 * @note 
 *	ncclResult_t
 *	ncclGather (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t sendcount (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int root (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t sendcount;
	ncclDataType_t datatype;
	int root;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclGather_t;

#define GET_ARGS_VALUE_ncclGather(activity) { \
	args_ncclGather_t* args = (args_ncclGather_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->sendcount = (size_t) sendcount; \
	args->datatype = (ncclDataType_t) datatype; \
	args->root = (int) root; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommWindowDeregister` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommWindowDeregister` function call.
 *
 * @struct args_ncclCommWindowDeregister_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommWindowDeregister (
 *			ncclComm_t comm (struct ncclComm *)
 *			ncclWindow_t win (struct ncclWindow *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	ncclWindow_t win;
	ncclResult_t retval;
} args_ncclCommWindowDeregister_t;

#define GET_ARGS_VALUE_ncclCommWindowDeregister(activity) { \
	args_ncclCommWindowDeregister_t* args = (args_ncclCommWindowDeregister_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
	args->win = (ncclWindow_t) win; \
};

/**
 * @brief Structure to hold the arguments for the `ncclSend` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclSend` function call.
 *
 * @struct args_ncclSend_t
 *
 * @note 
 *	ncclResult_t
 *	ncclSend (
 *			const void * sendbuff (const void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			int peer (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	size_t count;
	ncclDataType_t datatype;
	int peer;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclSend_t;

#define GET_ARGS_VALUE_ncclSend(activity) { \
	args_ncclSend_t* args = (args_ncclSend_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->peer = (int) peer; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommInitRankConfig` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommInitRankConfig` function call.
 *
 * @struct args_ncclCommInitRankConfig_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommInitRankConfig (
 *			ncclComm_t * comm (struct ncclComm **)
 *			int nranks (int)
 *			ncclUniqueId commId (struct ncclUniqueId)
 *			int rank (int)
 *			ncclConfig_t * config (struct ncclConfig_v22700*)
 *	)
 */
typedef struct {
	ncclComm_t * comm;
	struct {
		ncclComm_t  val;
	} comm__ref;
	int nranks;
	ncclUniqueId commId;
	int rank;
	ncclConfig_t * config;
	struct {
		ncclConfig_t  val;
	} config__ref;
	ncclResult_t retval;
} args_ncclCommInitRankConfig_t;

#define GET_ARGS_VALUE_ncclCommInitRankConfig(activity) { \
	args_ncclCommInitRankConfig_t* args = (args_ncclCommInitRankConfig_t*) activity->args; \
	args->comm = (ncclComm_t *) comm; \
	args->nranks = (int) nranks; \
	args->commId = (ncclUniqueId) commId; \
	args->rank = (int) rank; \
	args->config = (ncclConfig_t *) config; \
};

#define GET_PTRS_VALUE_ncclCommInitRankConfig(args) { \
	args_ncclCommInitRankConfig_t* pargs = (args_ncclCommInitRankConfig_t*) args; \
	if (pargs->comm != NULL) { \
		pargs->comm__ref.val = *pargs->comm; \
	} \
	if (pargs->config != NULL) { \
		pargs->config__ref.val = *pargs->config; \
	} \
};

/**
 * @brief Structure to hold the arguments for the `ncclCommDestroy` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclCommDestroy` function call.
 *
 * @struct args_ncclCommDestroy_t
 *
 * @note 
 *	ncclResult_t
 *	ncclCommDestroy (
 *			ncclComm_t comm (struct ncclComm *)
 *	)
 */
typedef struct {
	ncclComm_t comm;
	ncclResult_t retval;
} args_ncclCommDestroy_t;

#define GET_ARGS_VALUE_ncclCommDestroy(activity) { \
	args_ncclCommDestroy_t* args = (args_ncclCommDestroy_t*) activity->args; \
	args->comm = (ncclComm_t) comm; \
};

/**
 * @brief Structure to hold the arguments for the `ncclAllReduceWithBias` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclAllReduceWithBias` function call.
 *
 * @struct args_ncclAllReduceWithBias_t
 *
 * @note 
 *	ncclResult_t
 *	ncclAllReduceWithBias (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *			const void * acc (const void *)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	ncclRedOp_t op;
	ncclComm_t comm;
	int stream;
	void * acc;
	ncclResult_t retval;
} args_ncclAllReduceWithBias_t;

#define GET_ARGS_VALUE_ncclAllReduceWithBias(activity) { \
	args_ncclAllReduceWithBias_t* args = (args_ncclAllReduceWithBias_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->op = (ncclRedOp_t) op; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
	args->acc = (void *) acc; \
};

/**
 * @brief Structure to hold the arguments for the `ncclReduce` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclReduce` function call.
 *
 * @struct args_ncclReduce_t
 *
 * @note 
 *	ncclResult_t
 *	ncclReduce (
 *			const void * sendbuff (const void *)
 *			void * recvbuff (void *)
 *			size_t count (unsigned long)
 *			ncclDataType_t datatype (enum ncclDataType_t)
 *			ncclRedOp_t op (enum ncclRedOp_t)
 *			int root (int)
 *			ncclComm_t comm (struct ncclComm *)
 *			int stream (int)
 *	)
 */
typedef struct {
	void * sendbuff;
	void * recvbuff;
	size_t count;
	ncclDataType_t datatype;
	ncclRedOp_t op;
	int root;
	ncclComm_t comm;
	int stream;
	ncclResult_t retval;
} args_ncclReduce_t;

#define GET_ARGS_VALUE_ncclReduce(activity) { \
	args_ncclReduce_t* args = (args_ncclReduce_t*) activity->args; \
	args->sendbuff = (void *) sendbuff; \
	args->recvbuff = (void *) recvbuff; \
	args->count = (size_t) count; \
	args->datatype = (ncclDataType_t) datatype; \
	args->op = (ncclRedOp_t) op; \
	args->root = (int) root; \
	args->comm = (ncclComm_t) comm; \
	args->stream = (int) stream; \
};

/**
 * @brief Structure to hold the arguments for the `ncclResetDebugInit` function.
 *
 * This structure encapsulates the parameters and return value used in the 
 * `ncclResetDebugInit` function call.
 *
 * @struct args_ncclResetDebugInit_t
 *
 * @note 
 *	void
 *	ncclResetDebugInit (
 *	)
 */
typedef struct {

} args_ncclResetDebugInit_t;



/**
 * @brief Retrieves pointer-based argument values for RCCL API calls.
 *
 * This function extracts pointer-based arguments based on the given RCCL API ID.
 *
 * @param[in] id The RCCL API function identifier.
 * @param[in,out] args Pointer to the RCCL API arguments structure.
 * @param[in] is_enter Boolean flag indicating whether this function is handling an "enter" or "exit" event.
 */
static inline void get_rccl_pointed_args_for(rccl_api_id_t id, void* args, bool is_enter) 
{
    if (!is_enter) {
        switch(id) {
			case RCCL_API_ID_ncclGetLastError : {
				GET_PTRS_RET_VALUE_ncclGetLastError(args);
				return;
			}
			case RCCL_API_ID_ncclGetErrorString : {
				GET_PTRS_RET_VALUE_ncclGetErrorString(args);
				return;
			}
			case RCCL_API_ID_ncclGetVersion : {
				GET_PTRS_VALUE_ncclGetVersion(args);
				return;
			}
			case RCCL_API_ID_ncclGroupSimulateEnd : {
				GET_PTRS_VALUE_ncclGroupSimulateEnd(args);
				return;
			}
			case RCCL_API_ID_ncclCommCount : {
				GET_PTRS_VALUE_ncclCommCount(args);
				return;
			}
			case RCCL_API_ID_ncclCommShrink : {
				GET_PTRS_VALUE_ncclCommShrink(args);
				return;
			}
			case RCCL_API_ID_ncclCommInitRankScalable : {
				GET_PTRS_VALUE_ncclCommInitRankScalable(args);
				return;
			}
			case RCCL_API_ID_ncclCommRegister : {
				GET_PTRS_VALUE_ncclCommRegister(args);
				return;
			}
			case RCCL_API_ID_ncclCommGetAsyncError : {
				GET_PTRS_VALUE_ncclCommGetAsyncError(args);
				return;
			}
			case RCCL_API_ID_ncclCommInitAll : {
				GET_PTRS_VALUE_ncclCommInitAll(args);
				return;
			}
			case RCCL_API_ID_ncclCommCuDevice : {
				GET_PTRS_VALUE_ncclCommCuDevice(args);
				return;
			}
			case RCCL_API_ID_ncclCommSplit : {
				GET_PTRS_VALUE_ncclCommSplit(args);
				return;
			}
			case RCCL_API_ID_mscclRunAlgo : {
				GET_PTRS_VALUE_mscclRunAlgo(args);
				return;
			}
			case RCCL_API_ID_ncclCommUserRank : {
				GET_PTRS_VALUE_ncclCommUserRank(args);
				return;
			}
			case RCCL_API_ID_ncclRedOpCreatePreMulSum : {
				GET_PTRS_VALUE_ncclRedOpCreatePreMulSum(args);
				return;
			}
			case RCCL_API_ID_ncclAllToAllv : {
				GET_PTRS_VALUE_ncclAllToAllv(args);
				return;
			}
			case RCCL_API_ID_mscclLoadAlgo : {
				GET_PTRS_VALUE_mscclLoadAlgo(args);
				return;
			}
			case RCCL_API_ID_ncclMemAlloc : {
				GET_PTRS_VALUE_ncclMemAlloc(args);
				return;
			}
			case RCCL_API_ID_ncclGetUniqueId : {
				GET_PTRS_VALUE_ncclGetUniqueId(args);
				return;
			}
			case RCCL_API_ID_ncclCommInitRankConfig : {
				GET_PTRS_VALUE_ncclCommInitRankConfig(args);
				return;
			}
            default : break;
        }
    } else {
        switch(id) {

            default : break;
        }
    }
}
#endif // RCCL_API_HELPER_H