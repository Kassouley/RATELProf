/** THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY THE GILDA TOOL.
 * DO NOT MODIFY UNLESS YOU KNOW WHAT YOU ARE DOING.
 * ANY CHANGES MAY BE OVERWRITTEN BY SUBSEQUENT RUNS OF GILDA. 
 */
 
#include <ratelprof.h>
#include <ratelprof_ext.h>
#include "ompt_plugin.h"

#include "json_buffer.h"

void on_enter_ompt_callback(ratelprof_domain_t domain, ratelprof_api_id_t id, void* user_activity){
    ratelprof_ompt_api_activity_t* activity = (ratelprof_ompt_api_activity_t*)user_activity;
    ratelprof_activity_pool_push_activity(activity);
    get_correlation_id(&activity->corr_id);
    get_id(&activity->id);
    activity->phase = ratelprof_get_current_phase();
    activity->domain = domain;
    activity->funid = id;
    activity->pid = get_pid();
    activity->tid = get_tid();
    activity->start_time = ratelprof_get_curr_timespec();
}

void on_exit_ompt_callback(ratelprof_domain_t domain, ratelprof_api_id_t id, void* user_activity){
	(void)domain;
	(void)id;
	ratelprof_ompt_api_activity_t* activity = (ratelprof_ompt_api_activity_t*) user_activity;
    activity->stop_time = ratelprof_get_curr_timespec();
    pop_id();
}

void process_ompt_args_for(ompt_api_id_t funid, const ompt_api_args_t* args, void* user_args) 
{
    json_buffer_t* buf = (json_buffer_t*)user_args;
    switch(funid) {
        case OMPT_API_ID_target_data_alloc :
        case OMPT_API_ID_target_data_transfer_to_device :
        case OMPT_API_ID_target_data_transfer_from_device :
        case OMPT_API_ID_target_data_delete :
        case OMPT_API_ID_target_data_associate :
        case OMPT_API_ID_target_data_disassociate :
        case OMPT_API_ID_target_data_alloc_async :
        case OMPT_API_ID_target_data_transfer_to_device_async :
        case OMPT_API_ID_target_data_transfer_from_device_async :
        case OMPT_API_ID_target_data_delete_async :
			//	ompt_id_t* host_op_id (unsigned long);
			//	void* src_addr (void);
			//	int src_device_num (int);
			//	void* dest_addr (void);
			//	int dest_device_num (int);
			//	size_t bytes (unsigned long);
			//	const void* codeptr_ra (void);
			add_to_json_buffer(buf, "\"host_op_id\":{\"type\":\"ompt_id_t*\",\"value\":\"%p\"},", args->target_data_op.host_op_id);
			add_to_json_buffer(buf, "\"src_addr\":{\"type\":\"void*\",\"value\":\"%p\"},", args->target_data_op.src_addr);
			add_to_json_buffer(buf, "\"src_device_num\":{\"type\":\"int\",\"value\":\"%d\"},", args->target_data_op.src_device_num);
			add_to_json_buffer(buf, "\"dest_addr\":{\"type\":\"void*\",\"value\":\"%p\"},", args->target_data_op.dest_addr);
			add_to_json_buffer(buf, "\"dest_device_num\":{\"type\":\"int\",\"value\":\"%d\"},", args->target_data_op.dest_device_num);
			add_to_json_buffer(buf, "\"bytes\":{\"type\":\"size_t\",\"value\":\"%lu\"},", args->target_data_op.bytes);
			add_to_json_buffer(buf, "\"codeptr_ra\":{\"type\":\"const void*\",\"value\":\"%p\"}", args->target_data_op.codeptr_ra);
			break;

		case OMPT_API_ID_target_map :
			//	unsigned int nitems (unsigned int);
			//	void** host_addr (void);
			//	void** device_addr (void);
			//	size_t* bytes (unsigned long);
			//	unsigned int* mapping_flags (unsigned int);
			//	const void* codeptr_ra (void);
			size_t i = 0;
			size_t nitems = args->target_map_emi.nitems;
			add_to_json_buffer(buf, "\"nitems\":{\"type\":\"unsigned int\",\"value\":\"%u\"},", nitems);

			add_to_json_buffer(buf, "\"host_addr\":{\"type\":\"void**\",\"value\":[");
			for (i = 0; i < nitems; i++)
			{
				add_to_json_buffer(buf, "\"%p\",", args->target_map_emi.map[i].host_addr);
			}
			add_to_json_buffer(buf, "\"%p\"],", args->target_map_emi.map[i].host_addr);

			add_to_json_buffer(buf, "\"device_addr\":{\"type\":\"void**\",\"value\":[");
			for (i = 0; i < nitems; i++)
			{
				add_to_json_buffer(buf, "\"%p\",", args->target_map_emi.map[i].device_addr);
			}
			add_to_json_buffer(buf, "\"%p\"],", args->target_map_emi.map[i].device_addr);

			add_to_json_buffer(buf, "\"bytes\":{\"type\":\"size_t*\",\"value\":[");
			for (i = 0; i < nitems; i++)
			{
				add_to_json_buffer(buf, "\"%lu\",", args->target_map_emi.map[i].bytes);
			}
			add_to_json_buffer(buf, "\"%lu\"],", args->target_map_emi.map[i].bytes);

			add_to_json_buffer(buf, "\"mapping_flags\":{\"type\":\"ompt_target_map_flag_t*\",\"value\":[");
			for (i = 0; i < nitems; i++)
			{
				add_to_json_buffer(buf, "\"%s\",", get_map_flag_name(args->target_map_emi.map[i].mapping_flags));
			}
			add_to_json_buffer(buf, "\"%s\"],", get_map_flag_name(args->target_map_emi.map[i].mapping_flags));
			
			add_to_json_buffer(buf, "\"codeptr_ra\":{\"type\":\"const void*\",\"value\":\"%p\"}", args->target_map_emi.codeptr_ra);
			break;

		case OMPT_API_ID_target_submit :
			//	ompt_id_t* host_op_id (unsigned long);
			//	unsigned int requested_num_teams (unsigned int);
			add_to_json_buffer(buf, "\"host_op_id\":{\"type\":\"ompt_id_t*\",\"value\":\"%p\"},", args->target_submit_emi.host_op_id);
			add_to_json_buffer(buf, "\"requested_num_teams\":{\"type\":\"unsigned int\",\"value\":\"%u\"}", args->target_submit_emi.requested_num_teams);
			break;

        case OMPT_API_ID_target :
        case OMPT_API_ID_target_enter_data :
        case OMPT_API_ID_target_exit_data :
        case OMPT_API_ID_target_update :
        case OMPT_API_ID_target_nowait :
        case OMPT_API_ID_target_enter_data_nowait :
        case OMPT_API_ID_target_exit_data_nowait :
        case OMPT_API_ID_target_update_nowait :
			//	int device_num (int);
			//	const void* codeptr_ra (void);
			add_to_json_buffer(buf, "\"device_num\":{\"type\":\"int\",\"value\":\"%d\"},", args->target_emi.device_num);
			add_to_json_buffer(buf, "\"codeptr_ra\":{\"type\":\"const void*\",\"value\":\"%p\"}", args->target_emi.codeptr_ra);
			break;
 
        default : break;
    }
}

